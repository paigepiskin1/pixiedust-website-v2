-- Keep-original-outfit checkbox on all photoshoot templates is applied via
-- scripts/patch-shoot-keep-outfit.mjs and ensured at runtime by
-- ensureOptionalOutfitUpload() in src/lib/templates.ts.
SELECT 1;
